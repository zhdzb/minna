# 《大家的日本语》AI 学习系统重构方案

更新日期：2026-05-25

## 1. 文档目标

本文档用于指导当前项目从“题库练习工具”重构为“面向赴日工作的日语学习执行系统”。

本次重构的核心目标不是单纯增加题目数量，而是解决以下问题：

1. 每天打开系统后不知道该学什么，缺少明确方向。
2. 学习内容虽然围绕《大家的日本语》，但没有真正围绕“赴日工作所需的听说能力”来组织。
3. 当前练习更像刷题，没有把“教材学习、输入理解、输出训练、复习节奏、每日任务”串成闭环。
4. 用户需要的是一个能根据当前进度、掌握情况、当天可用时间，自动生成细致学习计划的系统。

---

## 2. 用户画像与当前约束

### 2.1 当前用户画像

- 年龄：28 岁
- 长期目标：30 岁前达到可以去日本找工作的日语能力
- 短期目标：在 2026 年内尝试 JLPT N3，甚至冲击 N2
- 当前教材：`《大家的日本语》`
- 当前进度：进行到第 22 课
- 当前痛点：前 1-22 课掌握不牢，尤其缺乏系统复盘
- 主要能力目标：`听`、`说`
- 当前现状：会读，但很难自己造句
- 使用场景：工作日学习时间不固定，可能是 1/2/3 小时，周末时间更多

### 2.2 对系统的真实需求

用户不是缺题，而是缺：

- 每日应该做什么
- 为什么今天做这些
- 今天做完算不算完成
- 哪些内容是为了赴日工作必须优先掌握的
- 哪些内容虽然学过，但还没进入可听懂、可开口、可复用的状态

结论：

当前系统的重构重点应从“练习生成器”转向“AI 驱动的每日学习计划系统 + 听说导向训练系统”。

---

## 3. 重构后的产品定位

### 3.1 新定位

当前项目应重构为：

`基于《大家的日本语》的 AI 日语学习执行系统`

它的职责不再只是出题和批改，而是：

1. 帮用户决定今天学什么。
2. 根据时间和当前阶段动态编排任务。
3. 把教材学习和系统训练联动起来。
4. 优先服务“赴日工作所需的听说能力”。
5. 通过可执行、可完成、可复盘的日计划，降低迷茫感和拖延感。

### 3.2 新产品原则

1. 系统必须给方向，不让用户自己决定今天做什么。
2. 系统必须以“学习闭环”为单位，而不是以“单题练习”为单位。
3. 系统必须优先强化听说，而不是继续偏重静态刷题。
4. 系统必须允许时间不固定，自动适配 30 分钟、60 分钟、120 分钟等不同节奏。
5. 系统必须能兼顾“重新夯实前 22 课基础”和“逐步推进后续课次”。

---

## 4. 对当前学习方法的判断

### 4.1 当前学习方法的优点

你现在的方法是：

1. 先看每一课语法
2. 读一读课文
3. 不做教材练习
4. 到当前系统中做练习

这个方法有几个优点：

- 输入和练习是分离的，不容易被纸质教材练习拖慢。
- 系统可以替代一部分传统练习册。
- 更适合用 AI 做动态训练。

### 4.2 当前方法的问题

这个方法最大的问题不是“做得不够多”，而是“中间缺了桥梁”：

1. 看语法和读课文之后，没有立刻转化成“可复述、可开口、可替换”的句型。
2. 系统虽然能出题，但没有把当天教材输入变成一套结构化训练流程。
3. 前面学过的内容没有被系统安排成节奏明确的回炉计划。
4. 听和说虽然是主要目标，但当前训练仍然以读题和写答案为主。

结论：

当前学习方法可以保留，但系统需要补上“输入转输出”的中间层。

---

## 5. 重构方向总览

本次建议按 5 个方向重构：

1. 新增“每日 AI 学习计划”模块，成为首页核心入口。
2. 重构练习体系，从“题型视角”转向“能力任务视角”。
3. 将《大家的日本语》课次学习拆成可执行的学习流。
4. 强化前 22 课回炉机制，建立“基础巩固带”。
5. 引入更细粒度的数据结构，让系统知道用户今天该学什么、练什么、复习什么。

---

## 6. 核心方案一：每日 AI 学习计划

### 6.1 这是本次重构的第一优先级

首页不应该先展示统计图，而应该先回答一个问题：

`你今天应该完成什么？`

因此首页需要新增一个主卡片：

`今日任务卡 / Today Mission`

### 6.2 计划生成逻辑

系统应基于以下输入生成当天计划：

1. 当前教材进度
2. 前 22 课基础掌握度
3. 最近错题与薄弱语法点
4. 最近几天训练记录
5. 用户当天可用时间
6. 用户当前目标阶段
7. 听说优先级

### 6.3 建议的计划生成方式

建议采用：

`规则引擎 + AI 生成` 的混合方案

而不是完全交给 AI 自由发挥。

原因：

1. 完全用 AI 生成日计划，容易不稳定，且执行口径不一致。
2. 你的计划需要长期可控、可复盘、可比较，必须有稳定骨架。
3. AI 更适合做“任务编排说明、当天重点解释、微调建议”，不适合完全负责排程规则。

### 6.4 推荐架构

#### A. 规则引擎负责

- 判断今天属于哪种学习日
- 计算基础复习 / 新课推进 / 听说强化 / 错题回炉 的比例
- 根据时间预算裁剪任务
- 决定任务顺序与最低完成线

#### B. AI 负责

- 给出当天学习计划说明
- 解释为什么今天安排这些任务
- 根据最近错误生成今日提醒
- 生成当天任务中的口语练习内容、情境内容、复述提示

### 6.5 每日计划的输出结构

系统每天应生成以下信息：

1. 今日目标
2. 今日总时长
3. 必做任务
4. 可选加练任务
5. 完成标准
6. 今日注意点
7. 学完后的复盘提示

示例：

```text
今日目标：
重新夯实第 5-6 课核心句型，并完成 1 次职场口语输出训练。

今日时长：
60 分钟

必做任务：
1. 复习第 5 课语法卡片 10 分钟
2. 跟读第 6 课课文音频 10 分钟
3. 完成 8 题核心句型训练 15 分钟
4. 完成 3 题口语替换造句 15 分钟
5. 完成 1 轮错题复盘 10 分钟

可选加练：
1. 再做 1 组听力关键词抓取训练
2. 对今天的 3 个句型做自由造句

完成标准：
1. 必做任务全部完成
2. 口语造句至少 3 题中 2 题达到可接受

今日注意点：
1. 重点区分助词「へ / に」
2. 不要求说得快，先保证句型完整
```

### 6.6 时间自适应策略

由于你的可用时间不固定，系统不能只支持一种计划长度。

建议最少支持 4 档：

1. `30 分钟`
2. `60 分钟`
3. `90 分钟`
4. `120 分钟及以上`

每档计划共用一套骨架，但任务数量和难度不同。

例如：

- 30 分钟：只保留核心复习 + 小量输出
- 60 分钟：复习 + 听说 + 错题回炉
- 90 分钟：增加新课推进或强化听力
- 120 分钟：加入完整的“输入 - 操练 - 输出 - 复盘”闭环

### 6.7 今日计划类型

建议定义几种“日计划模板”：

1. `基础巩固日`
2. `新课推进日`
3. `听说强化日`
4. `错题回炉日`
5. `周末长时训练日`

系统每天根据状态挑一个主类型，再做动态微调。

---

## 7. 核心方案二：题型与训练体系重构

### 7.1 对当前题型的判断

当前题型并非无效，但不够贴近你的真实目标。

目前项目里的题型更偏：

- 填空
- 翻译造句
- 对话补全

这些题型适合打基础，但如果目标是去日本工作，单靠它们不够。

### 7.2 新的训练设计原则

题目不应再只按“题型”分类，而要按“能力目的”分类：

1. 看懂
2. 听懂
3. 跟说
4. 替换说
5. 自己说
6. 复盘纠错

### 7.3 建议重构后的训练模块

#### 模块 A：语法理解训练

保留当前的：

- 助词 / 句型选择
- 填空
- 句意判断

用途：

- 检查是否真的理解课内语法

#### 模块 B：句型替换训练

这是你当前最缺的一层，应该重点新增。

形式：

1. 给一个标准句
2. 系统替换主语 / 时间 / 地点 / 宾语
3. 用户跟着句型重新说或写

例子：

标准句：

`わたしは 7じに かいしゃへ いきます。`

替换后：

- `田中さん / 9 点 / 学校`
- `あした / 東京 / 新幹線`

目标：

让你从“会看懂”变成“能套用”。

#### 模块 C：听力抓关键词训练

新增轻量听力训练，不追求正式考试风格，而追求实用理解。

形式：

1. 播放一句或一小段日语
2. 用户判断关键词、核心意图或补全信息
3. 强化工作和生活常见场景理解

目标：

先训练“听懂关键信息”，再训练“完整听写”。

#### 模块 D：跟读与影子跟读训练

当前项目已有 Web Speech/TTS 基础，可以继续强化。

建议加入：

1. 标准音频播放
2. 句子分段显示
3. 跟读提示
4. 自评选项

即使暂时不做语音识别评分，也应该先把“跟读任务”纳入每日计划。

#### 模块 E：情境口语输出训练

这是面向赴日工作的关键模块，建议替代一部分静态翻译题。

形式：

1. 给情境
2. 给角色
3. 给意图
4. 让用户说一句合适的话

例如：

- 你迟到了，要向前辈说明原因
- 你在公司询问会议时间
- 你向同事确认资料是否已经发送

目标：

逐步把《大家的日本语》的基础句型迁移到职场语境。

#### 模块 F：课文复述训练

建议新增。

因为你本来就会读课文，但还不会自己说。

可以设计成：

1. 先读课文
2. 系统提取关键词
3. 系统要求用更简单的句子复述
4. 再进入替换说和自由说

这会比直接让你“凭空造句”更容易过渡。

### 7.4 题型优先级调整建议

如果要服务赴日工作，建议将练习权重调整为：

1. `句型替换训练`
2. `情境口语输出`
3. `听力抓关键词`
4. `课文复述`
5. `语法理解填空`
6. `自由翻译造句`

说明：

- 纯翻译造句太难，容易卡住，适合作为进阶题，而不是主要入口。
- 对你来说，更适合从“模仿输出”逐步过渡到“自由输出”。

---

## 8. 核心方案三：围绕《大家的日本语》重构学习流

### 8.1 当前教材使用方式可以保留

建议保留你的学习习惯：

1. 看语法
2. 读课文
3. 在系统中练

但系统要把这三步接住，形成标准流。

### 8.2 建议的新学习流

每一课建议拆成以下 6 步：

1. `语法输入`
2. `课文输入`
3. `核心句型提取`
4. `句型替换训练`
5. `听说输出训练`
6. `错题与复盘`

### 8.3 系统内的课次任务模板

对于任意一课，系统应能自动生成：

1. 本课必须掌握的 3-5 个核心句型
2. 本课优先复用的高频词汇
3. 本课容易混淆的语法点
4. 本课适合的听说场景
5. 本课的最低输出标准

### 8.4 前 22 课的特殊策略

你的情况不适合简单继续往后推。

建议重构后采用：

`双轨制进度`

#### 轨道 A：基础巩固轨

- 针对第 1-22 课进行分组回炉
- 每天安排 1-2 个旧课复习任务
- 不要求完整重学，而是按句型和薄弱点回补

#### 轨道 B：主线推进轨

- 继续推进新课
- 但只有当基础巩固达到最低阈值时，才提高新课权重

建议默认策略：

未来 4-8 周内，以基础巩固为主，新课推进为辅。

---

## 9. 核心方案四：数据结构重构

当前数据结构主要记录进度、错题和备份，还不足以支撑“每日计划系统”。

建议新增以下结构。

### 9.1 用户学习画像

```json
{
  "user_profile": {
    "goal": "work_in_japan",
    "target_exam": "N3_or_N2",
    "target_date": "2028-12-31",
    "priority_skills": ["listening", "speaking"],
    "current_stage_lesson": 22,
    "foundation_restart_enabled": true
  }
}
```

### 9.2 每日计划记录

```json
{
  "daily_plan": {
    "date": "2026-05-25",
    "available_minutes": 60,
    "plan_type": "foundation_day",
    "focus_lessons": [5, 6],
    "tasks": [
      {
        "id": "task_1",
        "type": "grammar_review",
        "title": "复习第 5 课核心句型",
        "minutes": 10,
        "required": true,
        "status": "pending"
      }
    ],
    "ai_summary": "今天以巩固为主，重点回收助词和移动表达。"
  }
}
```

### 9.3 课次掌握度

建议不要只记录“是否完成”，而应记录多维掌握度：

```json
{
  "lesson_mastery": {
    "5": {
      "grammar": 0.65,
      "listening": 0.4,
      "speaking": 0.35,
      "reading": 0.7,
      "last_reviewed_at": "2026-05-24T10:00:00.000Z"
    }
  }
}
```

### 9.4 句型熟练度

如果后续希望把计划做细，建议再引入句型级别追踪：

```json
{
  "pattern_mastery": {
    "lesson_5_ni_ikimasu": {
      "lesson": 5,
      "pattern": "〜へ いきます",
      "recognition": 0.8,
      "controlled_output": 0.55,
      "free_output": 0.2
    }
  }
}
```

这会直接帮助系统判断：

- 你是“看得懂”
- 还是“能套用”
- 还是“能自由开口”

---

## 10. 核心方案五：页面与信息架构重构

### 10.1 首页重构

首页优先级建议改为：

1. `今日任务卡`
2. `当前学习阶段说明`
3. `基础巩固进度`
4. `本周完成情况`
5. `统计图表`

现有统计和热力图不需要删除，但不应该再占据首页最核心位置。

### 10.2 新增页面建议

#### 页面 A：今日计划页

功能：

- 选择今日可用时间
- 一键生成今日计划
- 查看今日任务列表
- 开始逐项执行

#### 页面 B：学习路径页

功能：

- 显示当前处于“基础回炉期 / 稳步推进期 / 冲刺期”
- 显示第 1-22 课掌握地图
- 显示下一阶段重点

#### 页面 C：课次训练页

将现有训练页进一步拆成：

- 句型训练
- 听说训练
- 错题回炉

#### 页面 D：周复盘页

功能：

- 本周完成了哪些任务
- 哪些任务经常没做完
- 哪些句型持续薄弱
- 下周建议重点

---

## 11. AI 方案建议：API 还是更好的方法？

### 11.1 结论

建议不是“纯 API”，也不是“纯本地规则”，而是：

`本地规则引擎为主，API 为增强层`

### 11.2 为什么不建议纯 API

如果把“制定每日计划”完全交给 API，会有几个问题：

1. 每天计划风格不稳定
2. 难以控制学习节奏
3. 成本和响应时间更高
4. 计划质量会受到 prompt 漂移影响
5. 长期复盘数据难以结构化比较

### 11.3 为什么也不能纯规则

如果完全不用 AI，只靠规则，也会有问题：

1. 计划缺少解释和鼓励
2. 不能自然生成当天的提醒和重点
3. 情境口语内容仍然需要 AI 辅助
4. 对“今天具体该练什么句子”的生成能力不足

### 11.4 推荐落地方式

#### 方案 A：每日计划生成

- 本地规则负责计划骨架
- API 负责生成说明文案、任务提示、口语情境、复习提醒

#### 方案 B：训练内容生成

- 基础训练可更多本地化或模板化
- 口语情境、复述提示、微型对话继续交给 AI

#### 方案 C：周复盘

- 本地聚合数据
- AI 输出“本周总结与下周建议”

这是当前项目最稳妥、最适合长期使用的路线。

---

## 12. 实施优先级建议

### Phase 1：先解决“今天学什么”

目标：

让系统具备最基本的方向感。

建议内容：

1. 新增 `今日计划页`
2. 新增可用时间选择
3. 新增规则驱动的每日任务生成
4. 首页改为展示今日任务卡
5. 增加基础巩固日 / 新课推进日 两种计划模板

### Phase 2：把训练从“刷题”改成“训练流”

目标：

让系统更贴近听说目标。

建议内容：

1. 新增句型替换训练
2. 新增课文复述训练
3. 新增听力关键词训练
4. 新增情境口语输出训练
5. 调整题型权重

### Phase 3：补上“基础回炉机制”

目标：

让前 22 课真正变成可用基础。

建议内容：

1. 设计前 22 课掌握度面板
2. 引入课次掌握度和句型掌握度
3. 增加基础巩固轨和主线推进轨
4. 让每日计划自动抽取旧课回炉任务

### Phase 4：做成真正的学习系统

目标：

让这个项目不只是练题，而是可长期陪跑。

建议内容：

1. 周复盘页
2. 学习路径页
3. AI 周总结
4. 长周期目标追踪

---

## 13. 推荐的 MVP 重构范围

如果要控制改造成本，建议先做一个最有价值的 MVP：

1. 首页新增“今日任务卡”
2. 新增“生成今日计划”逻辑
3. 支持输入今天可用时间
4. 支持 2 类计划模板
5. 新增“句型替换训练”
6. 新增“情境口语输出训练”
7. 为前 22 课加入简单掌握度字段

只要先完成这 7 项，项目的方向感就会明显提升。

---

## 14. 对你当前目标的具体策略建议

结合你当前情况，系统默认策略建议如下：

### 14.1 未来一阶段的主目标

未来 6-8 周内：

- 以第 1-22 课基础巩固为核心
- 重点从“会看”转向“会说”
- 每天都安排至少一个输出任务

### 14.2 每日计划的默认比例

工作日 60 分钟参考：

1. 旧课巩固：40%
2. 听说输出：35%
3. 错题复盘：15%
4. 新课推进：10%

周末 120 分钟参考：

1. 旧课巩固：35%
2. 听说输出：35%
3. 新课推进：20%
4. 复盘总结：10%

### 14.3 关于 N3 / N2 的现实建议

在 2026 年内尝试 N3 是合理目标。

直接以 N2 为主要短期目标风险较高，原因不是年龄或努力，而是你当前最主要的问题还在基础稳固和输出能力上。

因此系统层面的策略应是：

1. 先把前 22 课做实
2. 建立稳定输出能力
3. 逐步把训练迁移到更接近 N3 的听读与情境表达
4. 后续再评估 N2 冲刺内容是否独立加模块

---

## 15. 最终结论

这次重构最重要的不是“把系统做得更复杂”，而是让它真正成为你的学习指挥系统。

最关键的设计判断如下：

1. 首页必须从“统计展示”改成“今日任务驱动”。
2. 每日学习计划必须采用“规则骨架 + AI 增强”的混合方案。
3. 训练体系必须从“刷题”升级为“听说导向的训练流”。
4. 《大家的日本语》前 22 课需要建立基础回炉机制，而不是简单继续向后推。
5. 对你的目标而言，最优先提升的是“句型替换能力、情境表达能力、听力关键信息识别能力”。

---

## 16. 下一步开发建议

建议下一步直接进入产品设计与技术设计拆分，优先产出以下内容：

1. `每日 AI 学习计划` 的页面原型和交互流程
2. `daily_plan`、`lesson_mastery`、`pattern_mastery` 的数据结构设计
3. `规则引擎` 的生成逻辑文档
4. `句型替换训练` 和 `情境口语输出训练` 的题目结构设计
5. 首页重构方案

如果继续推进，我建议下一轮直接做两份后续文档：

1. 一份偏产品的 `PRD`
2. 一份偏开发的 `Technical Design`

这样你就可以直接开始进入实现阶段。

---

## 17. 部署、存储与安全补充方案

你补充的这部分非常关键，因为它会直接影响最终架构是否可长期使用。

本节专门回答 4 个问题：

1. 部署到 Vercel 能不能缓解本地 API 经常访问失败的问题
2. 部署到 Vercel 后，`data.json + git` 这套方式是否还能继续用
3. 当前两套 API 接入方式如何改造，才能保证 API Key 不暴露到公网
4. 如果要让 AI 参与总结和计划，如何避免上下文丢失

### 17.1 关于 Vercel 是否能缓解 API 访问问题

结论：

`大概率能改善，但不能只靠“部署上去”这一个动作，必须配合服务端代理架构。`

你现在在家里跑项目，经常遇到 API 访问问题，核心原因很可能不是前端本身，而是：

1. 你的本地网络访问外部模型服务不稳定
2. 你当前是浏览器直连模型 API，网络路径完全依赖你本机环境
3. 某些模型服务需要代理或 VPN 才能稳定访问

如果部署到 Vercel，并改成：

`浏览器 -> Vercel Function -> 模型 API`

那么请求将不再从你家里的浏览器直接打到模型服务，而是由 Vercel 的服务端函数去请求外部 API。

这通常会带来两个好处：

1. 你本地不再需要直接访问模型服务
2. API Key 不需要下发到浏览器

但是要注意：

1. 这不是 100% 保证，因为最终还要看目标模型服务是否允许 Vercel 机房出口访问
2. 如果模型服务本身对区域、出口 IP、供应商链路有额外限制，仍可能需要调整
3. Vercel Functions 默认运行区域未必最适合你的模型服务，需要按实际情况选择区域

所以更准确的判断是：

`Vercel + Serverless Proxy` 是正确方向，但需要做成正式的服务端代理，不是简单把静态前端丢上去。

### 17.2 当前的 data.json 方案在 Vercel 上是否可继续使用

结论：

`不能继续把 Vercel 上的 data.json 当成可持久写入的数据源。`

原因：

1. 你现在本地开发时，是通过 Vite 中间件把学习进度写回项目根目录的 `data.json`
2. 这种方式适合本地开发和本地 Git 同步
3. 但在 Vercel 的函数环境里，不能把运行时写文件当成持久化数据库来用

因此，部署到 Vercel 后，数据存储方案必须调整。

### 17.3 推荐的数据存储分层

建议分成两套存储策略：

#### 方案 A：本地单人优先版

适合你继续保留“自己的数据掌握在自己手里”的习惯。

- 本地运行时：继续允许 `data.json + git`
- Vercel 部署版：改用远程存储
- 两边不再强求完全共用同一种写入方式

这是迁移成本最低的方式。

#### 方案 B：统一云端版

如果你希望未来主要在 Vercel 上使用，那么建议逐步弃用运行时写 `data.json`，改为：

- 学习记录：远程数据库或对象存储
- 配置与轻量状态：数据库或 KV
- 本地导出备份：再导出成 JSON 文件

这会更适合长期使用。

### 17.4 存储选型建议

如果部署到 Vercel，推荐优先级如下：

1. `Postgres / Supabase / Neon`
2. `KV / Redis`
3. `Blob`

建议用途：

#### 用数据库存结构化学习数据

适合存：

- 用户画像
- 每日计划
- 进度
- 错题本
- 句型掌握度
- 周复盘

原因：

- 这些是结构化数据
- 需要查询、筛选、聚合
- 需要支持后续统计和 AI 汇总

#### 用 Blob 存 JSON 快照或导出文件

适合存：

- 每日快照
- 备份 JSON
- 导出学习档案

不建议把 Blob 当作主数据库来承载所有学习状态。

### 17.5 对当前项目最推荐的迁移策略

建议采用：

`短期双轨，长期统一`

#### 短期

1. 本地开发继续兼容 `data.json`
2. Vercel 线上版本改用远程存储
3. 增加“导出 JSON 备份”功能，保留你对数据的掌控感

#### 长期

1. 把 `data.json` 降级为备份格式
2. 把数据库升为主数据源
3. 将 git 同步从“运行时同步手段”改为“源码同步手段”

这会比继续试图在云端维持“可写 data.json”稳定得多。

### 17.6 当前两种 API 访问方式的重构建议

你提到当前应该有两种 API 访问方式，这个判断是对的。

结合当前代码，项目实际上是：

1. `浏览器直连模型 API`
2. `开发环境下通过本地代理转发`

这两种方式在本地开发阶段可以工作，但如果要正式部署，必须调整为：

`统一走服务端代理`

### 17.7 API Key 安全原则

要满足“API Key 不上传到公网”的目标，必须遵守下面这条原则：

`任何真实 API Key 都不能出现在前端可执行代码、localStorage、浏览器请求头、公开仓库文件里。`

这意味着：

1. 不能继续让浏览器直接带 key 请求 Gemini/OpenAI
2. 不能把 key 存在前端设置页并保存在浏览器本地
3. 不能把 key 放进 `config.js`
4. 不能把 key 提交到 git

### 17.8 推荐的 API 安全架构

部署到 Vercel 后，推荐改成：

1. 前端只调用你自己的 API 路由
2. Vercel Function 在服务端读取环境变量
3. Vercel Function 再去请求 Gemini 或 OpenAI

即：

`Browser -> /api/llm/* -> Vercel Env -> External LLM API`

这样做的好处：

1. API Key 不会暴露给浏览器
2. 前端仓库里不需要保存真实 key
3. 可以统一做超时、重试、限流、日志脱敏
4. 方便以后同时接 Gemini 和 OpenAI

### 17.9 两套模型接口的统一抽象

建议保留“双 provider”能力，但改造为统一后端接口。

例如：

- `/api/plan/generate`
- `/api/exercise/generate`
- `/api/exercise/evaluate`
- `/api/review/weekly-summary`

由服务端内部再决定：

- 用 Gemini
- 还是用 OpenAI
- 或者按任务类型路由到不同 provider

前端不应该再感知真实 provider key。

### 17.10 环境变量管理建议

建议在 Vercel 中使用环境变量管理：

- `GEMINI_API_KEY`
- `OPENAI_API_KEY`
- `OPENAI_BASE_URL`
- `DEFAULT_LLM_PROVIDER`

本地开发则使用：

- `.env.local`

并确保：

1. `.env.local` 在 `.gitignore` 中
2. 仓库中只保留 `.env.example`
3. 前端代码中不再从 `localStorage` 读取真实 key

### 17.11 AI 总结、计划与上下文丢失问题

这是非常重要的补充。

如果后续要让 AI 来做：

- 每日学习计划
- 今日总结
- 周复盘
- 薄弱点分析

那么绝对不能依赖“聊天历史”作为上下文。

必须采用：

`持久化学习上下文 + 每次请求显式组装 prompt`

### 17.12 为什么不能依赖对话上下文

原因有 4 个：

1. 模型 API 本身通常是无状态的
2. 即使某些平台支持会话，上下文也不稳定、不可控、成本高
3. 你的学习系统需要长期可追踪，不能把关键状态存在临时对话里
4. 每日计划和学习总结需要结构化数据，而不是模糊聊天记忆

### 17.13 推荐的上下文管理方式

每次请求 AI 时，服务端都从数据库或存储层显式读取并拼装：

1. 当前学习目标
2. 当前阶段
3. 当前教材课次
4. 近 7 天完成情况
5. 最近错题
6. 薄弱句型
7. 今日可用时间
8. 最近一次计划是否完成

然后把这些信息作为：

- `system prompt`
- `structured context payload`
- `task input`

一起发给模型。

### 17.14 建议新增的上下文快照结构

为了让 AI 稳定生成计划和总结，建议新增：

```json
{
  "learning_context": {
    "current_stage": "foundation_rebuild",
    "target_exam": "N3",
    "priority_skills": ["listening", "speaking"],
    "current_lesson": 22,
    "active_review_lessons": [5, 6, 9, 12],
    "recent_weak_patterns": [
      "〜へ いきます",
      "〜て ください"
    ],
    "last_7_days_summary": {
      "planned_minutes": 320,
      "completed_minutes": 210,
      "missed_tasks": 4
    }
  }
}
```

### 17.15 建议新增“AI 输入快照日志”

为了减少“AI 今天为什么这么安排”的不可解释性，建议在服务端记录：

1. 计划生成时使用了哪些上下文
2. 调用了哪个 provider
3. 使用了哪个 prompt version
4. 返回了什么结构化结果

这样好处很大：

1. 便于排查 AI 计划跑偏
2. 便于优化 prompt
3. 便于以后做版本回归
4. 便于控制成本

### 17.16 最推荐的最终架构

综合你的需求，最推荐的最终方向是：

`前端 Vue 应用 + Vercel Functions + 环境变量管理 + 远程结构化存储 + AI 上下文快照`

其中：

#### 前端负责

- 展示计划
- 执行任务
- 收集答案
- 展示结果

#### Vercel Functions 负责

- 调模型 API
- 读取环境变量
- 组装上下文
- 写入学习记录
- 生成计划和总结

#### 远程存储负责

- 进度
- 错题
- 每日计划
- 周总结
- 上下文快照

#### 本地 JSON 负责

- 导出备份
- 手工迁移
- 离线归档

### 17.17 对当前项目的明确决策建议

基于你补充的信息，建议把之前的重构方案再明确成下面这几个决策：

1. 不再把“运行时写 `data.json`”作为线上主方案。
2. Vercel 线上版必须改为“服务端代理模型请求”。
3. 所有真实 API Key 必须迁移到服务端环境变量。
4. AI 的计划、总结、复盘必须建立在显式持久化上下文之上。
5. `data.json + git` 继续保留，但定位改为“本地开发兼容 + 备份导出方案”。
